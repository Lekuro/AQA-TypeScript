### Install <https://www.npmjs.com/package/dotenv-safe>

```
npm i -D dotenv-safe
```

### To update some package (click on V letter and click on latest in package.json file, and npm i)

```
npm install eslint-plugin-unicorn@latest --save-dev
```

### install playwright with browsers

```
npx playwright install --with-deps
```
